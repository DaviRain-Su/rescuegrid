# RescueGrid — Build Status

日期：2026-06-03 · 环境：Sui Testnet

A running snapshot of what is built and how it was verified. Demo-facing summary for judges / handoff.

## What ships

| Layer | Status | Verified by |
| --- | --- | --- |
| **Web dashboard** (Vite + React) | ✅ Sui-only public scope | `npm run build` (2,420 modules); landing → zkLogin → dashboard, flash-crash demo; public Markets/Catalog/Profile/Data Sources hide non-Sui/CEX/bridge content for Hackathon submission |
| **Move package** `rescuegrid::policy` | ✅ deployed | `sui move test` 8/8; published `0x92f6e3…bb78` |
| **Worker API** (Cloudflare + Hono) | ✅ all endpoints | `npm test` + `npm run typecheck`; parse static Guardian block + runtime activity log + detail/list runtime stale reconciliation + runtime activity idempotency + activation/revoke preflight + ExecutorAdapter registry + adapter target gate + adapter SDK conformance + ChainDataProvider boundary + runtime signer/status surface + execution readiness endpoint + Sui protocol registry + Sui market watchlist + Sui adapter candidate constraints + Sui watch-only boundaries + Sui DEX/lending read adapters + active policy cap covered |
| **Sign-in** | ✅ | Sui wallet (Slush/std, no creds) primary; Enoki Google zkLogin option surfaced when configured |
| **Frontend ↔ Worker contract** | ✅ wired | live reads are Worker-first with direct-chain fallback; create/revoke use Worker-built unsigned txs; post-create activation passes the parsed strategy trigger into the policy runtime |
| **Live write loop** (create / list / revoke) | ✅ **verified on-chain** | real policy created (`9SQWkBne…`) + revoked (`Gzniih…`); endpoints return live data; post-revoke reads `Revoked` |
| **Live execution** (Deepbook order) | 🟡 gated | builders + dry-run; blocked on testnet DBUSDC funding |

## On-chain (testnet)

- rescuegrid package: `0x92f6e3218151e4d16fa51fd49df974a84ea744510f5e5a8ff79a01aacf27bb78`
- agent: `0x9eeed099…2ee43` · passport `0x0e7421…f8e6b` · BalanceManager `0x2e2e818f…aec2` (unfunded)
- reuses deployed MoveGate `0xec91e6…cf884a` · Deepbook `SUI_DBUSDC` pool `0x1c1936…7163a5`
- See `deployment.testnet.json` (`npm run config`).

## Phase ledger

- **B** feasibility — ✅ GO (`docs/B-feasibility-findings.md`): MoveGate Mandate shareable w/o owner co-sign, AuthToken hot-potato, Deepbook pools live.
- **C** Move — ✅ C1–C7. RescuePolicyWrapper + create/revoke/assert/record; thin helper builds `vector<TypeName>`; AuthToken consumed via MoveGate receipt; published.
- **E** Worker — ✅ E1–E9. parse (`executor_kind=deepbook`, unsupported strategy/executor errors, static slippage hard-cap `GUARDIAN_STATIC_BLOCK`, strategy_hash matches spec vectors) · create_policy PTB (zkLogin-signed, dry-run success) · activate only after wrapper/mandate liveness + strategy-hash preflight · active policy cap (`ACTIVE_POLICY_LIMIT_REACHED`) · list/detail activity (chain-authoritative + Durable Object runtime feed, chain-wins `runtime_state_stale` reconciliation) · Guardian · tick state machine now lives in `worker/src/runtime-core.js` and is reused by Worker tick + local daemon · Durable Object alarm · state sync · adapter registry with `UNSUPPORTED_EXECUTOR`, adapter target gating (`UNSUPPORTED_EXECUTOR_TARGET`) and adapter SDK conformance · signer adapter boundary (`worker-secret` implemented for Testnet Worker; `local-daemon` implemented only under `RESCUEGRID_DAEMON_MODE=true`; both now derive the secret public address and stay unavailable on `INVALID_SIGNER_SECRET` or `SIGNER_ADDRESS_MISMATCH`; `waap` is a local-daemon-only CLI adapter spike, disabled by default and unavailable in Cloud Worker mode, with JSON/NDJSON result parsing, explicit approval/policy/timeout non-success codes and runtime activity propagation via `signer_kind` / `approval_state`; `hardware`/`remote-signer` remain explicit `UNSUPPORTED_SIGNER` kinds).
- **F** Deepbook — 🟡 F1/F3 builders structurally verified (serialize + dry-run of create). `deepbook` ExecutorAdapter now produces an ExecutionPlan, exposes the H7 adapter SDK surface (`readMarket`, liquidity gate, volume gate, preview, unsigned PTB build, execution result parsing) and builds the unsigned PTB through the registry; F4 failure recovery/idempotency is covered at the runtime activity layer (same tx digest de-dupes, chain `AgentTradeExecuted` wins over runtime success rows, stronger success evidence can replace unresolved/error rows). Agent + BalanceManager are provisioned on-chain. **Blocked:** DBUSDC mint is permissioned and the SUI_DBUSDC pool is illiquid, so the BM can't be self-funded → live execution dry-run pending an external DBUSDC source. `EXECUTION_ENABLED=false` until then.
- **D** dashboard wiring — ✅ sign-in (Sui wallet primary, zkLogin optional); live create-policy (parse → build → wallet-sign → wait for `PolicyCreated` → activate with strategy trigger) and revoke wallet orchestration are extracted into `src/queries/wallet-flow.js` and covered by `npm run test:wallet-flow`; live D4 list/activity includes runtime stale markers + D5 revoke with `ALREADY_REVOKED` preflight; live policies/activity/summary/market/balances/runtime-status/execution-readiness poll every 5 seconds per `docs/05-test-spec.md`; expanded blocked activity now surfaces blocker code, observed value, required threshold, signer kind, approval state and signer blocker codes, with the ledger normalization covered by `npm run test:activity-ledger`; D6 Worker parse errors show an explicit strategy-builder error card instead of silently falling back to demo data; responsive + lazy-loaded landing; D3 live PTB preview separately surfaces owner, deployment agent, pool, budget, slippage and expiry; Active Strategy detail matches live runtime activity by wrapper id as well as display name; Policy Inspect now names the real MoveGate Mandate + `RescuePolicyWrapper` model instead of the older AgentPolicy/AgentCap/sponsored-gas story; Profile shows the Worker runtime signer, deployment agent, execution blocker, data-provider status, known signer kinds, WaaP/local-daemon external signer boundary and unified execution readiness from `/api/execution/readiness`. Data Sources, Markets and Strategy Catalog now consume Worker DEX/lending read surfaces, showing read-adapter counts, spread/health rows, strategy-template read models and execution blockers; Data Sources also consumes `/api/chain-data/status`, `/api/archival/replay-contract` and `/api/private-records/contract` to show provider posture, replay/private-record contracts, JSON-RPC fallback reads and storage-only blockers. Non-DeepBook templates open read-model detail instead of policy preview. **Live write loop proven on-chain** (see below).
- **G** packaging — ✅ G1 `npm run config`; G2 `npm run demo:loop` script covers create -> activate/monitor -> internal tick -> revoke -> post-revoke no-execution; `npm run demo:execute` is the strict same-loop validator: it calls `/api/execution/readiness` to preflight runtime signer status, BalanceManager DBUSDC/DEEP and agent SUI gas before policy creation, then fails unless the tick proves `AgentTradeExecuted`, `execution_claimed=true` and on-chain spend increase; G5 `npm run funding:request` produces a read-only external funding handoff with public BalanceManager / agent ids, DBUSDC/DEEP coin types, observed/required/missing balances and next verification commands; `npm run funding:watch -- --json` is the repeatable no-policy readiness watch, and `npm run funding:watch -- --wait --run-demo --worker-url http://localhost:8787` launches strict execution only after readiness is true; `npm run baseline:smoke` accepts `RESCUEGRID_FRONTEND_URL` / `RESCUEGRID_WORKER_URL` for non-default local ports and checks runtime status, chain-data status and execution readiness without leaking secrets; G3 README quickstart; this status doc. The execute leg currently fails at the documented DBUSDC/DEEP funding gate before creating a policy until F is unblocked.
- **H** composability — 🟡 local daemon scaffold exists (`npm run daemon -- status|policies list|watch list/add/remove/sync|tick|run|logs`) and runs the same Runtime Core module (`worker/src/runtime-core.js`) from the local process with JSONL activity logs, runtime boundary reporting, registered adapter reporting, owner policy listing, local watched-set persistence, local-agent mismatch checks, force-trigger demo gating and Mainnet external-signer gating. `npm run daemon -- status --json` now includes best-effort execution readiness via the same `worker/src/execution-readiness.js` helper used by `/api/execution/readiness`, so cloud Worker, local daemon and strict demo preflight share signer/funding blocker semantics; it also reports public external signer posture for the WaaP CLI spike without printing permission token values. `npm run daemon -- policies list --owner <0x...> --json` reads owner policies through the same ChainDataProvider boundary and marks watched wrappers; `npm run daemon -- watch sync --owner <0x...> --json` persists only active agent-matching wrappers into daemon config for later `run`. `local-daemon` signer mode signs with the local `AGENT_KEY` only when `RESCUEGRID_DAEMON_MODE=true` and the derived public address matches the deployed RescueGrid agent; `waap` signer mode is now a local-daemon-only spike that requires `--signer-kind waap --waap-cli-enabled --waap-sui-address <agent>` and hands serialized Sui `tx_json` to an injected `waap-cli send-tx` runner. The adapter handles single JSON and result-event NDJSON, and returns explicit `WAAP_APPROVAL_PENDING`, `WAAP_APPROVAL_DENIED`, `WAAP_POLICY_BLOCKED` and `WAAP_TIMEOUT` errors for non-submitted signer outcomes. Live submission still requires `--execution-enabled`, matching signer address evidence and a funded BalanceManager. `/api/runtime/status` now exposes non-secret cloud agent, signer public address, expected signer address, execution and data-provider posture for the Profile accounts surface. `hardware`/`remote-signer` remain unsupported until their external signer spikes are validated. H3 protocol registry baseline is implemented at `/api/protocols`: DefiLlama Sui `chainTvls.Sui` top 26 plus Turbos as an explicit DEX volume exception, with every entry scoped to `chain=sui` and only DeepBook marked as the live executor. H3 market watch data is implemented at `/api/protocols/watchlist`: 31 Sui-only market rows across spot, lending, LST, CDP, vault, RWA and perps; DeepBook rows expose `executor_configured=true` but `execution_enabled=false` / `execution_blocker_code=FUNDING_GATED` until DBUSDC/DEEP funding is real. H4/H5 adapter constraints are implemented at `/api/adapters/candidates`: Cetus, Turbos, Momentum, Bluefin Spot, NAVI, Suilend, Scallop and AlphaLend have Sui-only target schemas, read fields, preflight gates and wrapper-change requirements, with every row `registered_executor=false` and `execution_enabled=false`. H6 watch-only boundaries are implemented at `/api/protocols/watch-boundaries`: Bucket, Current, SpringSui, Haedal, Volo, AlphaFi, Kai, Mole, Ondo, KAIO, MatrixDock, Ember, Bluefin Pro, Sudo and DipCoin expose readable state, risk domains, required future target fields and explicit no-execution reasons. H7 adapter SDK skeleton is implemented in `worker/src/executor-adapter-sdk.js`; DeepBook has been split into `worker/src/deepbook-adapter.js`, and `worker/src/executor-adapters.js` is now only the registered-adapter assembly point. H8 DEX read adapters are implemented at `/api/adapters/dex-reads`: 5 adapters, 8 supported market rows and a 10-row SUI/USD-stable spread matrix, all non-executable except DeepBook's existing funding-gated execution adapter. H9 lending read adapters are implemented at `/api/adapters/lending-reads`: 4 adapters, 5 supported market rows and a 4-row borrow-health matrix; Suilend/Scallop are read-only and NAVI/AlphaLend remain research-pending read-only.
- **J** Sui data / signer hardening — 🟡 J1/J2 ChainDataProvider boundary is partially implemented. `JsonRpcChainDataProvider` remains the default provider, and Worker read endpoints, `runTick` and Durable Object price monitoring resolve reads through `worker/src/chain-data-provider.js`. `GraphqlChainDataProvider` now exists as a configurable read-only spike for wrapper/mandate snapshots, policy-module events, owner policy lists, owner activity and owner summary; it is disabled unless `CHAIN_DATA_PROVIDER=graphql` plus `SUI_GRAPHQL_URL` / `SUI_GRAPHQL_ENDPOINT` or an injected transport is configured. `/api/chain-data/status` now exposes the selected provider, transport, read model and optional bounded schema/read probe; Data Sources displays the same status so GraphQL fallback reads are visible. `npm run chain-data:status -- --json` is the local/CI diagnostic for the same boundary, and `--provider graphql --owner <0x...> --wrapper-id <0x...> --json` compares GraphQL owner policy/activity output against JSON-RPC without printing endpoint URLs. J3 monitoring posture is now explicit: `timer-polling` remains the active Durable Object alarm tick driver, while `MONITORING_PROVIDER=grpc` reports `GRPC_MONITORING_NOT_IMPLEMENTED` and cannot change the execution hot path yet. J4 archival replay contract is implemented at `/api/archival/replay-contract` for `historical_activity`, `performance_replay` and `judge_demo_replay`; provider default is `none`, and `archival-store` remains `not_validated` even when endpoint posture is configured. J5/J6 private policy record contract is implemented at `/api/private-records/contract` for Seal + Walrus `strategy_snapshot`, `backtest_report`, `agent_reasoning_trace` and `incident_report`, plus a Sui Stack CRM-style future `PolicyPrivateRecord` shared object contract with reader ACL, version table, optimistic `expected_current_version`, operation contracts and plaintext-free event contracts; provider default is `none`, `seal-walrus` remains `not_validated`, no Move mutation endpoint exists yet, and signing secrets / raw hidden reasoning are disallowed payload fields. J7 signer posture is partially implemented as a status/adapter boundary: `worker-secret` and `local-daemon` now validate the secret-derived public address against the deployed agent before reporting availability; `waap` now has a test-covered local-daemon CLI adapter boundary with JSON/NDJSON parsing plus approval/policy/timeout state codes, and remains unavailable by default / in Cloud Worker mode; `hardware` and `remote-signer` remain visible `UNSUPPORTED_SIGNER` kinds. J8 engineering setup is implemented in `docs/10-engineering-setup.md`, mapping Sui Agent Skills to data, PTB, Move, publish, frontend and Walrus Sites workflows plus validation commands and guardrails. Balance, gas and DeepBook market reads still fall back to JSON-RPC, live GraphQL endpoint enablement still needs running the probe against the current official Sui GraphQL endpoint, live gRPC streaming still needs a reviewed Runtime Core/Durable Object integration, live Archival Store replay still needs provider validation, live Seal/Walrus private records still need provider validation and Move implementation, and live WaaP execution still needs an authenticated WaaP session plus approval/privilege validation. External signer production hardening remains future work.
- **K** frontend product breadth — 🟡 K2/K3/K4 are implemented as Sui-only read/advisory product surfaces. K5 Strategy Builder v2 now consumes the shared adapter-readiness helper and blocks final deploy for read-only or missing adapters instead of creating fake non-DeepBook strategies. K6 Active Strategy Detail v2 shows live legs, net exposure, PnL/carry attribution, open orders, execution ticks, pending approvals, Guardian limits and pause/resume/rebalance/revoke/export controls; the inventory drawer is fixed for Sui venue nodes. K7 Risk Center now shows global budgets, strategy pause/resume, Sui venue stop/resume, venue caps, liquidation/oracle/live runtime signer health, stale-data warnings, Guardian simulation and capability boundaries; live signer warnings are derived from `/api/runtime/status` when available, including WaaP's local-daemon-only external signer boundary. Live global / strategy / venue stops are owner-signed Sui personal-message controls persisted in the Worker runtime and enforced as `GLOBAL_STOPPED`, `STRATEGY_STOPPED` or `VENUE_STOPPED` pre-submit blockers. K8 Agent Ledger v2 now normalizes demo/live activity rows into strategy, Sui venue, status, Guardian, signer block, approval and tx/order dimensions, with filter controls and expanded audit evidence for reason, input snapshot, PTB plan, signer kind, approval state, signer blocker codes, tx/order id, PnL and budget impact.

## Live write loop — verified on-chain (2026-06-02)

Broadcast with the dedicated agent key (agent-as-owner for the test):
- `create_policy` → tx `9SQWkBneN2jZ1ovETaZyRkx8UrwntSUBceBwPT2gRYdP` → wrapper `0x85703d17…55ea4`, mandate `0x1587a441…33825`.
- Worker endpoints returned it live: `/api/policies?owner=` (1), `/api/activity?owner=` (PolicyCreated), `/api/policies/:id/activity` (`Monitoring`, spent 0).
- `revoke_policy` → tx `GzniihEkpUNJG3dr1K1e5J3f5YWJ75B6yBnYMvmWEfmg` → `PolicyRevoked`; post-revoke reads `revoked:true` / `runtime_state: Revoked`, activity = 2 events.

So create / list / activity / revoke are real on testnet. In the browser, a connected Sui wallet drives the same Worker-first flow (no DBUSDC needed); read-only screens can fall back to direct Sui/DeepBook reads when `VITE_WORKER_URL` is absent.

## Baseline smoke — verified locally (2026-06-03)

With Worker `http://localhost:8787` and frontend `http://localhost:5173` running:

- `RESCUEGRID_FRONTEND_URL=http://localhost:5173 RESCUEGRID_WORKER_URL=http://localhost:8787 npm run baseline:smoke` passed.
- `npm run chain-data:status -- --json` passed with the default JSON-RPC provider and `probe.status=skipped`; `npm run chain-data:status -- --probe --json` passed against Sui Testnet clock reads with `provider_status=ready`.
- Evidence covered deployment id consistency, `.env.local` Worker URL, Worker service root, `/api/runtime/status`, `/api/chain-data/status`, `/api/archival/replay-contract`, `/api/private-records/contract`, frontend Vite env, Sui Testnet fullnode, RescueGrid package, agent passport, BalanceManager, DeepBook `SUI_DBUSDC` pool and Testnet indexer.
- Runtime status evidence covered `sui:testnet`, deployment agent match, known signer kind, execution config mirroring `.dev.vars`, Worker-first data provider and no `AGENT_KEY` / `INTERNAL_AGENT_TICK_TOKEN` value leakage.
- Funding gate stayed explicit: BalanceManager `DBUSDC_raw=0`, `DEEP_raw=0`, `EXECUTION_ENABLED=false`, and the smoke asserted `EXECUTION_DISABLED` with no execution tx submitted.

## Known gaps / next

1. **DBUSDC funding** — the only true remaining gap, and **self-funding is confirmed impossible** on this testnet: DBUSDC `mint` is TreasuryCap-gated (cap not public), DEEP `mint` returns `FunctionNotFound` on the current package, and a SUI→DBUSDC swap needs DEEP for taker fees (a zero-DEEP swap fills 0 even with a live bid). Needs an **external DBUSDC source** (DeepBook-team faucet, or an address that already holds DBUSDC/DEEP). Run `npm run funding:request` for the current public BalanceManager id, coin types and missing amounts to send to the funding provider. Once the agent BalanceManager holds DBUSDC/DEEP: flip `EXECUTION_ENABLED=true`, run `npm run funding:watch -- --json` to verify readiness without creating a policy, then use `npm run funding:watch -- --wait --run-demo --worker-url http://localhost:8787` or `npm run demo:execute`; strict execution will replay the execution PTB through `worker/src/executor-adapters.js` and fail unless the chain emits `AgentTradeExecuted` with spend increase.
2. **Browser wallet click-through** — connect Slush (testnet) and run create/revoke from the UI against `VITE_WORKER_URL`. The frontend wallet orchestration is now mock-signer tested, including `PolicyCreated.wrapper_id` extraction and runtime activation, but a real extension click-through is still the final browser evidence.
3. **zkLogin live test** (optional) — the Enoki Google option is surfaced and helper-tested when configured; real OAuth click-through still needs browser evidence if using Enoki instead of a standard wallet.
